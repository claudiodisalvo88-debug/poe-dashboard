# RQ-031 Critic D-only Aggregate Review

## Status

CRITIC REVIEW / RESEARCH-FRAMING ONLY

## Result

REFINE_REQUIRED

## Scope checked

This review checks `RQ-031` and the Innovator output only as D-only aggregate research-framing.
The checked object remains only possible aggregate relation / aggregate composition among multiple primitively admissible reputational evidence units.

## Critic assessment

The Innovator `ACCEPT_WITH_CONSTRAINTS` is directionally useful but not yet safe enough for direct carry-forward.
The current wording still leaves residual ambiguity because `aggregate relation` can still be read as hidden rule structure and `aggregate composition` can still be read as hidden formula structure.
The plurality of evidence units also keeps open interpretive pressure toward grouping, comparison, ordering, weighting and dependency-shaped reasoning.
The correct next step is PM refinement, not documentation-readiness.

## Residual leakage risks

* aggregate rule: `relation` can still drift into hidden rule semantics about how units must be combined
* aggregate score: plurality and composition can still drift into hidden scalar synthesis
* aggregate formula: `composition` can still be read as formula-shaped assembly language
* ranking / ordering: multiple units can still imply comparative placement, sequence or precedence
* weighting: plurality can still imply hidden relevance balancing among units
* dependency graph computation: relation/composition can still imply dependency mapping or graph-shaped computation
* validation / admissibility: aggregate language can still drift into sufficiency, eligibility, inclusion or exclusion logic
* evidence grouping: multiple units can still imply bounded group object, bundle object or readiness package
* schema / API / database / dashboard: grouping language can still drift into storage, payload, endpoint or UI grouping structure
* implementation: aggregate wording can still drift into runtime processing or system behavior language
* hidden formula-candidate leakage: aggregate composition can still pressure future formula-candidate shaping
* hidden readiness-gate leakage: grouped evidence can still be read as precondition package for later steps

## Required refinement or constraints

`RQ-031` must be refined so that aggregate survives only as conceptual placeholder language and not as compositional structure.
`Relation` must not imply rule, dependency, ordering or admissibility semantics.
`Composition` must not imply formula structure, score assembly, evidence bundle, grouped object or readiness package.
`Multiple primitively admissible reputational evidence units` must not imply comparison, weighting, ranking, ordering, grouping schema, validation path or implementation grouping.
Until that refinement is made, `RQ-031` should not move to documentation-readiness.

## Non-authorizations

This review does not answer `RQ-031`.
This review does not validate `RQ-031`.
This review does not close `RQ-031`.
This review does not create `PD-031`.
This review does not define formula candidate.
This review does not define aggregate rule, aggregate score or aggregate formula.
This review does not authorize implementation.

## Effect on open research

`RQ-026`, `RQ-027`, `RQ-028`, `RQ-029`, `RQ-030` and `RQ-031` remain OPEN / RESEARCH-FRAMING with Decision `NONE`.
