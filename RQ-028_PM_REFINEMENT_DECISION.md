# RQ-028 PM refinement decision

Status:
OPEN / RESEARCH-FRAMING ONLY

Source:
M-002-RQ-028-PM-REFINEMENT

Reviewed files:

* RQ-028_INNOVATOR_RESEARCH_HANDOFF_LEAKAGE_REVIEW.md
* RQ-028_CRITIC_HANDOFF_BOUNDARY_LEAKAGE_REVIEW.md

PM decision:
ACCEPT_WITH_CONSTRAINTS

Core PM finding:
The Critic constraints are accepted.
`RQ-028` can proceed toward documentation-readiness only if the wording is refined so that safe handoff is not treated as protocol object, handoff package, handoff template, readiness milestone, formula-preparation layer, standard schema, operational step, checklist, gate, classifier, dependency graph or implementation-facing structure.

Required wording refinement:
Replace the operational implication of safe handoff with:
research-boundary carry-forward

Meaning:

* research outputs may be carried forward only as non-binding research context
* provenance may be remembered only as source lineage, not as formal requirement
* negative constraints may be preserved only as leakage-prevention memory, not as inverse checklist
* `RQ-026` and `RQ-027` must not become coupled logical inputs
* `RQ-028` must not authorize future formula candidates

Strong PM boundary:
`RQ-028` does not say how to perform handoff.
`RQ-028` says how not to transform research-framing into protocol structure.
`RQ-028` is not a protocol object.
`RQ-028` is not a readiness gate.
`RQ-028` is not a template.
`RQ-028` is not a package.
`RQ-028` is not a formula-preparation layer.
`RQ-028` is only a leakage-prevention boundary.

Constraints:

* `RQ-028` remains OPEN / RESEARCH-FRAMING
* Decision remains `NONE` in `RESEARCH_QUEUE.md`
* no `PD-028`
* no `PD-027`
* no `PD-026`
* no `RQ-026` closure
* no `RQ-027` closure
* no formula candidate
* no final formula
* no formula structure
* no formula syntax
* no weights
* no coefficients
* no scalar score
* no total score
* no scoring
* no ranking
* no ordering
* no checklist as protocol rule
* no classifier
* no gate
* no threshold
* no operational filter
* no dependency graph algorithm
* no dependency graph construction
* no dependency computation
* no incentives
* no tokens
* no payout
* no economic allocation
* no API logic
* no database logic
* no dashboard logic
* no runtime logic
* no implementation

Next required step:
Open `M-002-RQ-028-DOCUMENTATION-READINESS` as active task.
The task must decide whether `RQ-028` can be documented as research-framing complete without closing `RQ-028` and without creating `PD-028`.
